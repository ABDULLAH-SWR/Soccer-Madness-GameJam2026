public static class GameSettings
{
    // GAME MODE
    public static bool isPvE = false; // true = Play vs PC, false = 2 Players

    // FORMATION (We will use this later)
    // 0 = Standard (2-1-2)
    // 1 = Attack (1-1-3)
    // 2 = Defense (3-1-1)
    public static int formationIndex = 0;
}